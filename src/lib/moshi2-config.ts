/**
 * 有料模試(第2回)の仕組みを、他の資格ドリルへ移すときに書き換える唯一のファイル。
 *
 * 【接頭辞をサイトごとに変える理由】
 * cookie は同じドメイン配下(shikakumon.com とそのサブドメイン)で共有されうる。
 * 接頭辞が同じだと、筋トレで買った受験権が別サイトでも通ってしまう(署名鍵が
 * 同じ場合)。サイトごとに変えておけば、名前の時点で衝突しない。
 * 実際に設備へ移植したとき、この書き換えを忘れたまま公開直前まで気づかなかった
 * ため、prebuild の監査で検査するようにしてある。
 */

export const MOSHI2_CONFIG = {
  /** 受験権 cookie の接頭辞。サイトごとに必ず変える。kt_(筋トレ)/st_(設備)/sk_(本体) */
  cookiePrefix: "sk_m2_",

  /** 購入メールの差出人。ドメイン認証済みのアドレスを使うこと */
  mailFrom: "シカクモン <noreply@shikakumon.com>",

  /**
   * 問い合わせ先。エラー文面と購入メールに出る。
   * お問い合わせページ(/contact/)に載せている宛先と揃えてある。
   * kintore@ / setsubi@ のようなドメインのアドレスに変えたい場合はここだけ直す。
   */
  contactEmail: "dawuzhangguchuan131@gmail.com",
} as const;
